package wipro.day5.Maps;

import java.util.Comparator;
import java.util.Map;
import java.util.Map.Entry;
 
public class MyComparator implements Comparator<Map.Entry<Long, Game>> {
 
	@Override
	public int compare(Entry<Long, Game> o1, Entry<Long, Game> o2) {
		// TODO Auto-generated method stub
		return o1.getValue().getgRank().compareTo(o2.getValue().getgRank());
	}
 
}
