server:
  servlet:
    context-path: /
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/appdb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true
  jackson:
    serialization:
      write-date-keys-as-timestamps: false  

